inputSet:
  name: is1
  identifier: is1
  orgIdentifier: default
  projectIdentifier: Git_Sync_Project
  pipeline:
    identifier: pipeline_1
    description: null
    stages:
    - stage:
        identifier: s1
        type: Deployment
        spec:
          serviceConfig:
            serviceDefinition:
              type: Kubernetes
              spec:
                manifests:
                - manifest:
                    identifier: m1
                    type: K8sManifest
                    spec:
                      store:
                        type: Github
                        spec:
                          branch: master
            serviceRef: <+pipeline.name>
          infrastructure:
            infrastructureDefinition:
              type: KubernetesDirect
              spec:
                connectorRef: <+pipeline.name>
            environmentRef: <+pipeline.name>
      parallel: null
