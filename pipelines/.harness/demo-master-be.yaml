inputSet:
  name: demo-master-be
  identifier: demomasterbe
  pipeline:
    identifier: demo_pipeline
    description: null
    stages:
    - stage:
        identifier: be
        type: Deployment
        spec:
          infrastructure:
            infrastructureDefinition:
              type: KubernetesDirect
              spec:
                namespace: demo-master
                releaseName: demo-master
      parallel: null
